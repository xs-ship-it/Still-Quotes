import { UpvoteDownvoteDirective } from './upvote-downvote.directive';

describe('UpvoteDownvoteDirective', () => {
  it('should create an instance', () => {
    const directive = new UpvoteDownvoteDirective();
    expect(directive).toBeTruthy();
  });
});
